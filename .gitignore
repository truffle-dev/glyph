# Built registry output. Regenerated by `go run ./tools/build`.
# Deployed copy lives in the docs site repo.
/r/

# Local build artifacts
/dist/
*.svg.tmp

# Go workspace state
/vendor/
*.test
*.out

# Editor
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Stray local binaries from go build at repo root
/glyph
/story
/showcase
/reel

# Visual recordings — regenerate locally with `asciinema rec` or vhs.
# Source-of-truth lives in visuals/tapes/. Rendered output is not committed.
/visuals/casts/
/visuals/out/
