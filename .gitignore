# Built registry output. Regenerated by `go run ./tools/build`.
# Deployed copy lives in the docs site repo.
/r/

# Local build artifacts
/dist/
*.svg.tmp

# Go workspace state
/vendor/
*.test
*.out

# Editor
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Stray local binaries from go build at repo root
/glyph
/story
/showcase
/reel
/file-explorer
/chat-cli
/dashboard
/log-viewer

# Visual recordings. Source-of-truth lives in visuals/tapes/.
# Casts are intermediate (regenerate locally via visuals/render-cast.sh);
# the reel cast is the only one committed because it has a stable tape.
/visuals/casts/*
!/visuals/casts/reel.cast

# Rendered gallery GIFs ship in-repo so the README gallery resolves on
# github.com. Regenerated with `bash visuals/render-cast.sh`.
