# Built registry output. Regenerated by `go run ./tools/build`.
# Deployed copy lives in the docs site repo.
/r/

# Local build artifacts
/dist/
*.svg.tmp

# Go workspace state
/vendor/
*.test
*.out

# Editor
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Stray local binaries from go build at repo root
/glyph
/story
/showcase
/reel

# Visual recordings. Source-of-truth lives in visuals/tapes/. Most
# rendered output is regenerated locally and not committed, but the
# headline reel (cast + gif) is the hero asset and ships in-repo.
/visuals/casts/*
!/visuals/casts/reel.cast
/visuals/out/*
!/visuals/out/reel.gif
